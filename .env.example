# Öffentliche URL der App (wird für QR-Code verwendet)
# Lokal: weglassen oder http://localhost:3000
# Auf Railway/Render: automatisch gesetzt via PUBLIC_URL
PUBLIC_URL=https://deine-app.railway.app

# Port (wird von Railway/Render automatisch gesetzt)
PORT=3000
